<<<<
  const [activeTab, setActiveTab] = useState<'work' | 'play' | 'cast' | 'learn'>('work');
  const [showCastDisclosure, setShowCastDisclosure] = useState(false);

  const handleTabClick = (tabId: 'work' | 'play' | 'cast' | 'learn') => {
    if (tabId === 'cast') {
      setShowCastDisclosure(true);
    } else {
      setActiveTab(tabId);
    }
  };
====
  const [activeTab, setActiveTab] = useState<'work' | 'play' | 'cast' | 'learn'>('work');
  const [isAppsMenuOpen, setIsAppsMenuOpen] = useState(false);
  const [showCastDisclosure, setShowCastDisclosure] = useState(false);

  const handleTabClick = (tabId: 'work' | 'play' | 'cast' | 'learn') => {
    setIsAppsMenuOpen(false);
    if (tabId === 'cast') {
      setShowCastDisclosure(true);
    } else {
      setActiveTab(tabId);
    }
  };
>>>>
